import { DocsPlaceholder } from "@/pages/docs/DocsPlaceholder";

export default function DocsRegrasPage() {
  return (
    <DocsPlaceholder
      titulo="Regras de alertas"
      descricao="As regras automáticas que o motor avalia a cada ciclo de coleta e seus thresholds configuráveis."
    />
  );
}
